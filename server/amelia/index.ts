import type { Hono } from 'hono';
import type { ServerDependencies } from '../../shared/contracts';

/** Lane D replaces this scaffold without changing the import in server/index.ts. */
export function registerAmeliaRoutes(_app: Hono, _deps: ServerDependencies): void {}
